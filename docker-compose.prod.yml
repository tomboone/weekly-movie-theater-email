services:
  app:
    image: ghcr.io/tomboone/weekly-movie-theater-email:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - MOVIE_STATE_PATH=/app/data/movie_state.json
    volumes:
      - app-data:/app/data

volumes:
  app-data:
